Options -MultiViews
RewriteEngine On

# Keep real files and directories (assets, favicon, .well-known) untouched.
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Let React Router handle application routes.
RewriteRule ^ index.html [L]
